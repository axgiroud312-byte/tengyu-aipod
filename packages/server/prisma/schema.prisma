datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Placeholder {
  id String @id @default(cuid())

  @@map("_placeholder")
}
