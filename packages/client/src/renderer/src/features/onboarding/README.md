Onboarding feature folder.

The step UI still lives in `App.tsx` during M1 so the route shell can land without changing the
activation and setup flow behavior. Later milestones can move the step panels here.
